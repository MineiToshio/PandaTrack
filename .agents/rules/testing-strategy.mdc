---
description: Evaluate and add unit, integration, and E2E tests according to feature risk instead of aiming for blanket coverage
alwaysApply: true
---

# Testing strategy – PandaTrack

Every implementation must explicitly evaluate whether it needs **unit tests**, **integration tests**, and/or **E2E tests**.

Do not aim for blanket coverage. Aim for **risk-based coverage** on business-critical behavior.

**Tests must reflect the current implementation.** If they get out of sync with the code, they lose value and can mislead. Review and update tests in the same change that modifies the behavior they cover.

## Required evaluation on every change

Before finalizing a feature, bugfix, or refactor:

1. **Review existing tests** in the affected scope. If the change alters behavior that existing tests assert, **update those tests in the same change** so they match the new behavior. Do not leave tests that no longer reflect what the code does.
2. **Decide whether to add** unit, integration, and/or E2E tests (see below). If a test type is not added, that should be because it is **not justified by the change**, not because it was skipped by default.

## When each test type is expected

### Unit tests

Add unit tests when the change includes:

- business rules, calculations, totals, balances, status resolution, or date logic
- validation helpers, parsers, formatters, mappers, or branching logic
- bug fixes that can regress at function level

### Integration tests

Add integration tests when the change includes:

- server actions, route handlers, or query/data modules with meaningful logic
- Zod validation + server logic + persistence working together
- interactions between Prisma/data access and application rules
- third-party integrations where request/response handling is important

### E2E tests

Add E2E tests when the change affects:

- core MVP workflows
- navigation or form flows whose value is only proven end-to-end
- App Router behavior involving async server rendering, redirects, or cross-page state transitions
- regressions that are easiest to catch from the user perspective

## App Router guidance

For Next.js App Router, do not rely on component tests alone for async Server Component behavior. Prefer a small number of focused E2E tests for critical end-to-end flows.

## Priority areas for PandaTrack

Prioritize tests for:

- purchase and item tracking rules
- pre-order payment calculations and remaining balance
- shipment and split-shipment behavior
- dashboard totals and status summaries
- trust/store signals if they influence product decisions or filters

## What not to optimize for

- Do not require 100% coverage.
- Do not add shallow tests for trivial markup with no meaningful logic.
- Do not add brittle tests tied to implementation details instead of user behavior or stable contracts.
- Do not assert exact user-facing UX copy in tests unless that wording is itself the requirement.

## Copy assertions

- Prefer assertions on stable behavior:
  - element visibility
  - ARIA roles such as `alert` or `status`
  - URLs and redirects
  - enabled/disabled state
  - side effects such as analytics calls or submitted payloads
- Treat normal product copy as volatile. A copy refresh should not break tests for otherwise-correct behavior.
- Only assert exact text when the wording is contractual, legal, compliance-related, or intentionally fixed as part of the acceptance criteria.

## Validation expectation

When finalizing a change, run the relevant automated tests so that updated or new tests are executed, not only written. See the **validation checklist** rule for the full order of commands (`npm run test`, type-check, lint, validate-build, and E2E when applicable). When test infrastructure exists for the affected scope, run at least the tests that cover the changed behavior.

## E2E database hygiene (required)

E2E tests must not leave persistent test data in the database. This applies everywhere the suite
runs — dev, staging, or production — never only "because it's not prod."

- Cleanup must run from `test.afterEach` (or `afterAll` for suite-level fixtures), never only as
  the last synchronous step inside the test body. Playwright still runs `afterEach` when a test
  fails or times out; inline end-of-test cleanup does not, so a single failed assertion above it
  orphans everything the test created. Track created ids/slugs in a variable the test populates as
  it creates each record, and have `afterEach` sweep whatever is still tracked.
- If the feature under test also mutates real account state (base currency, preferences, and
  similar), track the original value the same way and restore it from `afterEach` too — a leaked
  mutation on the shared dev account is as much a hygiene violation as a leaked row.
- Prefer a direct-DB delete (see `e2e/_helpers/dbCleanup.ts`) as the cleanup mechanism, not a
  UI-driven flow: a UI delete can itself be broken by the same failure that skipped the test's own
  cleanup step, or may not exist at all (for example a moderation "remove" action is a soft
  delete — `status: REJECTED` — never a hard delete; only a direct DB call removes the row). A
  spec may still exercise UI-driven delete as part of its own coverage, but `afterEach` must not
  depend on it succeeding.
- Prefer deterministic cleanup keys (for example captured ids, slugs, or a unique fixture-value
  prefix like a push-subscription endpoint) over broad deletes.
- If you intentionally verify persistence, still delete the created records before the suite ends.
- Never leave E2E-generated rows, or E2E-driven mutations of real rows, as permanent state in any
  shared environment.

## Test file placement

- Co-locate tests with the feature they protect.
- For multi-file components or feature folders, place test files in a dedicated **`_tests/`** folder inside that feature folder.
- Put Playwright specs in a top-level **`e2e/`** folder and split them by domain or workflow area such as `landing.spec.ts`, `auth.spec.ts`, or `dashboard.spec.ts`.
- Do not group unrelated domains into one large E2E spec file when separate domain files would keep the suite easier to scan.
- Use **`src/test/`** only for shared test infrastructure such as render helpers, fixtures, factories, or reusable mocks.
- Avoid mixing large numbers of `*.test.ts(x)` siblings directly with implementation files when a feature already has its own folder.
