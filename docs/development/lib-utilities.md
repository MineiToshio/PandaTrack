# `src/lib` Utilities Index

This file is the source of truth for shared app-level utilities in `src/lib/`.

## Purpose

- Help developers quickly find the right utility file.
- Keep responsibilities clear (what belongs in each file).
- Reduce duplicated helper logic.

## Organization rules

- Use domain folders inside `src/lib/` when there are at least two files that belong to the same concern.
- Current grouped concerns are `src/lib/auth/`, `src/lib/analytics/`, `src/lib/catalog/`, `src/lib/imageIntake/`, `src/lib/images/`, `src/lib/integrations/`, `src/lib/pwa/`, `src/lib/store/`, `src/lib/user/`, and `src/lib/user-settings/`.
- Keep a file in the `src/lib/` root only when it is cross-domain or does not have a close sibling category yet.

## Inventory

| File                                                      | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/a11y/focusable.ts`                               | Focusable-element query selector and focus options shared by modal overlays (skip `aria-hidden` and `tabindex="-1"` nodes).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/a11y/useFocusScope.ts`                           | Client hook: Escape to close, Tab focus trap, and focus restoration for dialogs and drawers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/analytics/posthog-server.ts`                     | Server-side PostHog client helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/analytics/posthogDataAttributes.ts`              | Declarative PostHog data-attribute helpers for clickable UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/app-url.ts`                                      | Resolve app base URL by environment (local vs Vercel).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/auth/auth-client.ts`                             | Better Auth browser client helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/auth/auth-server.ts`                             | Server-side session retrieval helper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/lib/auth/auth.ts`                                    | Better Auth server configuration (providers, verification hooks).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/lib/auth/authPasswordRecovery.ts`                    | Password recovery request/throttle helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/auth/authRedirect.ts`                            | Safe auth callback/return URL resolution and auth links.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/lib/auth/authVerification.ts`                        | Verification lifecycle state helpers (grace/block/reminder).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/auth/authVerificationEmail.ts`                   | Auth verification email business logic (locale resolution, i18n content selection, template composition inputs).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `src/lib/auth/accountCapabilities.ts`                     | Derives settings account capabilities (email change, password change, set password) from linked Better Auth provider ids.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/lib/auth/usernameChangeCooldown.ts`                  | Seven-day cooldown helpers for successful username changes (`User.usernameChangedAt`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/auth/emailChangeRateLimit.ts`                    | Server-side cooldown for successful email-change requests (7-day window, persisted via `verification` rows).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/auth/authEmailChangeSecurityEmail.ts`            | Localized transactional email sent to the previous address after Better Auth accepts an email change (informational only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/catalog/collectorCountries.ts`                   | Seeded collector-market `COUNTRY_CODES`, `COUNTRY_FLAG_EMOJI_BY_CODE` / `getCollectorCountryFlagEmoji`, primary ISO 4217 currency per country, and allowlist helpers for settings validation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `src/lib/catalog/storeProductTypes.ts`                    | Shared seeded `STORE_PRODUCT_TYPE_KEYS` catalog and allowlist helper for store/product-type validation across seed and user settings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/lib/constants.ts`                                    | Shared app constants (routes, analytics events, contact info, Cloudflare asset routes, collector shell layout classnames `APP_SHELL_*`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/lib/currency.ts`                                     | Money formatting (`formatAmount`, `formatAmountSymbolOnly`, `formatAmountWithSymbol`) plus the currency-exponent layer: uniform `MINOR_UNITS_PER_MAJOR` (×100) storage, `getCurrencyDecimals` / `isZeroDecimalCurrency` (0 for CLP/JPY/KRW, 2 otherwise), and `isWholeMajorAmount`. Formatters render decimals per exponent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/decimalInput.ts`                                 | Currency-aware decimal input helpers used on every price/payment `onChange`: `sanitizeDecimalInput(value, currencyCode?)` (limits the fraction to the currency exponent, truncating at the separator for zero-decimal currencies) and `isValidPositiveDecimal(value, currencyCode?)`. Omit the code for non-currency decimals such as the FX rate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/lib/money/parseDecimalToMinorUnits.ts`               | Strict server-side decimal-string → integer minor-units parser (`parseDecimalToMinorUnits(value, currencyCode?)`). Rejects malformed input and a decimal separator for zero-decimal currencies; scales uniformly ×100.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/money/majorAmountToMinorUnits.ts`                | Numeric sibling of the parser above, for a boundary that hands over a JSON number instead of a string (the AI extraction response): `majorAmountToMinorUnits(59.9)` is `5990`. Rounds the ×100 product through a fixed-precision step first, so binary noise (`1.005 * 100` is `100.49999999999999`) cannot round a half-subunit the wrong way. Uniform ×100, so `1200` JPY is `120000`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/lib/fonts.ts`                                        | Font loading and font config helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/lib/data/deliveries/deliveryMutations.ts`            | Delivery-domain data mutations. Owns `createDelivery()`, the lifecycle transitions (`markDeliveryDelivered`, `reopenDelivery`, `cancelDelivery`, `deleteDelivery`), `editDelivery` (membership + metadata with atomic revalidation), `updateDeliveryNote`, product state transitions, and `persistDerivedOrderStatuses()` inside the same transaction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/data/deliveries/deliveryQueries.ts`              | Delivery-domain read helpers. Owns store-scoped eligibility grouping, stores-with-eligible-products for standalone create, source-order context for from-order create, the paginated deliveries list query (`getDeliveriesList`), the list filter-drawer store options, and the full detail query (`getDeliveryDetail`, items grouped by source order).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/lib/deliveries/deliveryIdentifier.ts`                | Human-readable delivery id generation (`DLV-YYYYMMDD-NN`) scoped by user and day.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/lib/deliveries/deliveryListSort.ts`                  | Deliveries list sort options (`oldest` default per FR-08-30, `recent`, `eta-asc`, `store-asc`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/deliveries/deliveryState.ts`                     | Pure delivery product-state helpers and mapping from persisted `OrderItemDeliveryState` to order-status derivation states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/deliveries/deliveryValidation.ts`                | Zod schemas and inferred types for delivery create, edit, lifecycle actions, delete, and private-note updates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `src/lib/data/imageIntake/imageIntakeMutations.ts`        | `reserveImageIntakeUsage` / `settleImageIntakeUsage`: writes a `PENDING` `ImageIntakeUsage` row with an estimated cost before the provider call and increments the collector's `ImageIntakePeriod` roll-up by the whole batch, after checking the hard-stop ceiling, the per-user rate limit, the monthly photo bag (per-user override read inside the lock, or uncapped for an administrator) and the daily anti-burst cap. Either every photo fits or nothing is written. Settlement replaces the row with the real tokens and cost, gives the photos back on `FAILED`, and corrects the roll-up cost. Both run inside a transaction guarded by a Postgres advisory lock on `periodKey`; settlement reports the period total immediately before/after so callers can detect an alert-threshold crossing without a racing second query.                                                                                                                                                                         |
| `src/lib/data/imageIntake/spendGuard.ts`                  | `createImageIntakeSpendGuard`: builds the ledger-backed `SpendGuard` the extraction engine requires. `assertCanSpend` reserves a `PENDING` row with an estimated cost and the submission's photos, mapping a bag refusal to `SpendGuardBlockedError("quota-exceeded"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | "daily-cap-exceeded")`carrying the remaining balance;`recordUsage`/`recordFailure`settle it to`SUCCEEDED`or`FAILED`, so a billable failure still counts toward the ceiling. Parses the spend-guard env (alert/hard-stop USD, optional alert email) with Zod, fails closed on an unreadable ledger or a failed settlement, and dispatches a Resend email to `IMAGE_INTAKE_ALERT_EMAIL` on an alert-threshold crossing evaluated under the same settlement lock. |
| `src/lib/data/imageIntake/imageIntakeQuotaQueries.ts`     | `getImageIntakeQuotaSnapshot` (plus a request-scoped `cache()`d variant) reads one collector's balance for the current period from two unique-key reads: the per-user override and the period roll-up. `searchImageIntakeQuotaAccounts` backs the administrator's override console, matching username or email with a capped result set and each account's usage for the period.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `src/lib/data/imageIntake/imageIntakeQuotaMutations.ts`   | `setImageIntakeQuotaOverride`: sets or clears `User.aiMonthlyPhotoLimit` and writes the `imageIntake.quotaOverride` audit row in the same transaction, so a change can never land without its trail. Reason is required; the limit is bounded by `MAX_MONTHLY_PHOTO_LIMIT_OVERRIDE`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lib/imageIntake/quota.ts`                            | Pure quota arithmetic shared by the reservation path and every counter surface: `formatPeriodKey` / `formatDayKey` (UTC), `nextPeriodStart` (the renewal date the exhausted copy states), `resolveEffectiveMonthlyLimit` (override ?? default, `null` for an administrator), `computeRemainingPhotos`, `computePhotoOverflow` (how many photos to remove for the batch to fit), and `buildQuotaSnapshot`. No Prisma, no I/O.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/imageIntake/constants.ts`                        | Shared order image intake limits: submission/file size and count ceilings, image dimension bounds, the 200-product ceiling, accepted MIME types, the client compression targets (`1080x2400`, WebP `q0.85` / JPEG `q0.90`, 10% segment overlap), and the photo-quota figures (`DEFAULT_MONTHLY_PHOTO_QUOTA` 20, `DAILY_PHOTO_CAP` 10, `MAX_MONTHLY_PHOTO_LIMIT_OVERRIDE`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/imageIntake/draftSchema.ts`                      | Strict Zod schema for `ImageIntakeDraft` (store, currency, order date, total, groups, payments, delivery, warnings), including the `Field<T> = { value, source: "read" \| "assumed" \| null }` provenance wrapper; `delivery` is the minimal `{ expectedFrom, expectedTo, cost } \| null` shape. `parseImageIntakeDraft` rejects unknown properties.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lib/imageIntake/prompt.ts`                           | Builds the static system prompt (Peruvian collector glossary, breakdown-rule instructions, currency/date rules, "never fill silently" instruction, and an explicit images-are-data-not-instructions prompt-injection boundary). Interpolates only Zod-validated `baseCurrency`, `now`, and `locale`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lib/imageIntake/extractionEngine.ts`                 | Provider-agnostic `extract()` boundary: runs the spend guard, calls the `ExtractionProvider` with a 30 s timeout and at most one retry on `ProviderTransportError`, validates the response against `draftSchema`, and records usage. `computeCostMicroUsd` prices Gemini 3.1 Flash-Lite at USD 0.25 / USD 1.50 per million input/output tokens.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/imageIntake/geminiProvider.ts`                   | The one production `ExtractionProvider`, calling `@google/genai` with `thinkingConfig.thinkingLevel: ThinkingLevel.MINIMAL` and a hand-written `Schema` mirror of `draftSchema.ts`. Refuses to run unless `IMAGE_INTAKE_PAID_TIER_CONFIRMED === "true"` and outside a browser environment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/imageIntake/productNameCase.ts`                  | `capitalizeProductName` raises only the first letter of an extracted product name, leaving the rest as read (title casing would be wrong in Spanish and would destroy casing the source got right, like "One Piece" or "PS5"), and returns a name that does not start with a letter untouched. `withCapitalizedProductNames` applies it across a draft on the extraction path only: a name the collector types themselves is their own choice, and `sourcePhrase` stays verbatim because it is quoted back as evidence. Server-side rather than a prompt instruction, for the same reason the amount contract is: a mechanical transform belongs where it is deterministic.                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/imageIntake/mapDraftToOrderCreate.ts`            | Pure structural mappers from a confirmed `ImageIntakeDraft` to `orderCreateSchema` / `orderPaymentCreateSchema` input shapes. Does not call Prisma or validate; `orderCreateSchema.safeParse` at save time is the single validation point.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/imageIntake/breakdown.ts`                        | `applyBreakdown`: deterministic post-processing of a parsed draft, applying the price split per group (`explicit-unit`, `divided-lot` by integer division with remainder to the first products, or `none`), zero-decimal currency handling, and the 200-product ceiling as a discriminated `product-ceiling-exceeded` outcome, never a silent truncation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/lib/imageIntake/validateUpload.ts`                   | `validateUploadedImages`: server-side upload validation using `sharp.metadata()` (real header-byte format, never the declared MIME type), image count, per-file and total byte ceilings, and dimension bounds, including an explicit `empty-submission` code for a zero-image submission, and `image-too-small` / `image-too-large` as separate codes (a source with too few pixels and a decompression-bomb guard are different problems with different remedies). Every per-file failure carries the offending index, and a dimension failure also carries the measurement, so the caller can name the photo and quote its size. Validates and measures only; never re-encodes.                                                                                                                                                                                                                                                                                                                                |
| `src/lib/imageIntake/clientPrecheck.ts`                   | `precheckAttachedPhotos` (photo **count** only, on the photos as attached), `countPhotosWithinSubmissionBudget` (how many photos, from the first, fit the request budget, so a refusal can end in a number) and `precheckPreparedSegments` (count plus per-file and total byte size, on the segments preparation produced). Pure and client-safe (no `sharp`, no server-only API), reusing the subset of `validateUpload.ts`'s error codes each can detect without decoding. A source photo is never refused for its own byte size: preparation is what decides the size of the upload, so the ceilings apply to its output. Earliest-feedback UX only; not a security boundary, the server always re-validates.                                                                                                                                                                                                                                                                                                 |
| `src/lib/imageIntake/diagnostics.ts`                      | `reportImageIntakeFailure` / `countPartialResponseShape`: the one place an intake failure is recorded, as a single greppable `[image-intake]` server line plus one Sentry event. Zero retention means nothing about a failed read survives the request, so what is not captured here is unrecoverable. Numbers, enums and fixed codes only, never content; `countPartialResponseShape` counts occurrences of the response schema's own key names, so it measures a cut-off answer's shape without reading its text. Never throws.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/lib/imageIntake/segmentPlan.ts`                      | `planIntakeSegments` / `projectIntakeOutputDimensions`: the pure geometry of the client image pipeline (width normalisation to `1080px`, and the overlapping vertical split of a tall screenshot). Shared by `compressForIntake`, which draws the segments, and `dimensionPrecheck`, which has to know what will be produced before anything is drawn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/imageIntake/dimensionPrecheck.ts`                | `checkIntakeSourceDimensions` / `minIntakeSourceHeight`: pure, client-safe verdict on one photo from its decoded size, projected through `segmentPlan`. Separates `source-too-small` (a thumbnail) from `source-too-wide` (a wide, short crop that only fails because the normalisation to `1080px` drops its height under the minimum), and reports the minimum source height that photo's own width would need. Earliest-feedback UX only; the server stays the authority.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `src/lib/images/canvasEncoding.ts`                        | `supportsWebpEncoding` (real encoder-support probe) and `encodeCanvasToBlob` (encodes and verifies the actual produced blob type, falling back to JPEG when the requested WebP type does not come back as WebP). Fixes the Safari canvas encoder silently returning PNG for a requested WebP output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lib/images/compressForIntake.ts`                     | `compressForIntake` (one photo), `compressBatchForIntake` (a whole batch, in order, at most three decodes at a time so an unbounded batch cannot exhaust a phone's memory) and `prepareSubmissionForIntake` (the batch plus the quality ladder: when the prepared submission does not fit in one request it is prepared again at a lower WebP quality, dimensions untouched, stopping at the first rung that fits): client-side image compression for order image intake. Caps dimensions to `1080x2400`, never upscales, splits screenshots taller than the cap into overlapping segments, and always returns the re-encoded output, never the original file, flagging `recompressedLarger` when a recompressed candidate is not smaller. Also reports the source's own decoded dimensions, the only place in the flow that has them.                                                                                                                                                                           |
| `src/lib/images/readImageDimensions.ts`                   | `readImageDimensionsFromUrl`: measures an attached photo from the object URL its preview already uses, so the attach surface can judge it without a second object URL or a second decode. Resolves `null` on anything undecodable or slow, never rejects.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/lib/images/getCroppedImageDataUrl.ts`                | Client-side canvas helper: renders a crop area of an image and returns a preview data URL. Shared between store logo and avatar crop-confirm flows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/integrations/GoogleAppsScript.ts`                | Google Apps Script integration for waitlist persistence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/lib/localDate.ts`                                    | Local-time date helpers (`addDays`, `startOfMonth`, `endOfMonth`, `toIsoDateString`) shared by list filter and form components. Promoted from `orders/_utils` when the deliveries list reused it. Server-side query boundaries use UTC instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/integrations/kit.ts`                             | Kit.com API integration (subscriber/tag operations). Syncs authenticated users with `app_user` tag (non-blocking).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/lib/integrations/resend.ts`                          | Generic Resend email utility (provider sending plus reusable transactional email HTML template). Supports optional `bodyHtml` for styled inline links; `buildTransactionalMailtoLink` uses the same accent as verification fallback links.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/og.ts`                                           | OG image helpers and localized OG text helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/prisma.ts`                                       | Prisma client singleton and adapter setup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/data/progression/accrual.ts`                     | The credit call sites: one exported function per anchor (`creditOrderCreation`, `creditOrderPayment`, `creditDeliveryReceived`, `creditOrdersCompleted`), each applying the `BR-12-07` store gate, pricing the fact at the moment it happens (the anti-split ladder position, `FR-12-07`), and appending through `awardPointsBatch` inside the host mutation's own transaction after its last refusal (`FR-12-12`, `ADR 0022`). Every one swallows its own failure and returns rows appended or `null`; `combineCredits` folds several steps and poisons the result to `null` if any failed, so a partial delta never reaches the client. `settleProgression` runs AFTER the host transaction commits and turns that count into `{ pointsDelta, rankUp, medalsUnlocked }` by re-deriving the cache, never by summing the rows just written (`FR-12-13`, `ADR 0037`).                                                                                                                                             |
| `src/lib/data/progression/rankLadder.ts`                  | The ten-rank ladder (`FR-12-14`): `pointsForRank` (the approved threshold curve), the derived `RANK_LADDER` constant (`rankKey` + `threshold`, merit-lock fraction on ranks 9-10), and `deriveRank`, which resolves a point total and the two album counters to a `currentRankIndex` and whether the merit lock is satisfied (`FR-12-17`, `ADR 0038`). Pure and dependency-free (the progression money guard scans it): `shippedMedalCount` arrives as a plain number the caller already read from `medalCatalogue.getShippedMedalCount()`. Permanence (`highestRankIndex` never decreasing, `BR-12-06`) is `recompute.ts`'s responsibility, not this module's.                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/data/progression/storeCreditEligibility.ts`      | The single definition of `BR-12-07`'s gate, in all three shapes it is needed in: `isStoreCreditEligible` (row predicate), `STORE_CREDIT_ELIGIBILITY_SELECT` (the exact `select` a host mutation must widen its own store lookup to) and `CREDITABLE_STORE_FILTER` (the same gate as a nested Prisma filter, for the queries that push it down). Its own module because the recompute, the medal evaluator and every write path need the identical answer; copies would eventually disagree and a collector would watch points appear and then vanish. The gate reads approval and visibility only: since 2026-08-23 it no longer asks who registered the store, and the select deliberately omits `createdByUserId` so a creator clause cannot be reintroduced without also reintroducing the field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/lib/data/progression/medalCatalogue.ts`              | The 24-medal catalogue across six series, and the pure rules over it. A LEAF like `pointRules.ts` (the progression money guard scans it): it imports nothing and a medal declares the CONDITION it needs by key rather than as a predicate. Twelve entries are phase 1 and awardable; the other twelve are catalogued and render as silhouettes but are excluded from every counter and from the evaluator (`FR-12-20`). Exports `getShippedMedalCount()` (the album's denominator) and `getMeritLockDenominator(unlockedKeys, now)` (the rank ladder's, which drops medals the collector cannot control and closed event windows, `FR-12-17`), plus `selectUnlockedMedals` (pure, deterministic order) and `resolveConditionsToEvaluate` (which conditions are still worth a query). Every row carries `publicSafe`, `stateful`, `secret`, `controllable`, the phase-3 event columns, and `imageKey`, the single substitution point for medal artwork. Nothing here may ever return a point value (`ADR 0040`). |
| `src/lib/data/progression/medalEvaluation.ts`             | The database side of the catalogue: exactly one resolver per condition key, plus `evaluateUnlocks` (called by `recompute.ts`, replacing its stub) and `resolveStatefulMedalCurrency` (the `"vigente"` / `"ya no vigente"` line, computed at read time and never a revocation). Resolves only the conditions still worth asking about, so an album that is almost full costs almost nothing. `BR-12-07`'s store gate is pushed into every query rather than applied afterwards, as the shared `CREDITABLE_STORE_FILTER`, money reaches `first-order-closed` as a boolean from `moneyPredicateAdapter.ts`, and `first-photo-order` reads the call-time context instead of scanning notes (`ADR 0040`). Scanned by the progression money guard with an explicit import allowlist.                                                                                                                                                                                                                                                                            |
| `src/lib/data/progression/medalQueries.ts`                | Read side of the album: `getMedalAlbum(userId)` (the catalogue joined with this collector's unlocks, grouped into six pages, global and per-series counters over the SHIPPED count) and `getMedalDetail(medalKey, userId)` (one medal plus obtainability and the next unheld piece of the same series; `null` for an unknown key, which the route turns into a 404). Also `getMedalShowcase(userId, limit)`, the newest few unlocks plus the two counters the dashboard widget and the `Resumen` vitrine print, reading exactly those rows instead of walking the catalogue. Never a bare catalogue lookup: answering "is this unlocked" without a `userId` would be answering it for somebody (`BR-12-02`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lib/data/progression/progressionQueries.ts`          | Read side of the points domain, and what the `Progreso` section renders. `getUserProgressCache` / `listUserPointLedger` / `resolveProgressionOccurredOn` sit beside `getProgressSummary(userId)`, which returns the cached total and rank, the next threshold and the points still missing, the bar measured BETWEEN the two thresholds bounding the current rung (never from zero, the curve is superlinear), this civil month's breakdown by rule group, the merit-lock counter once rank 6 is reached, and whether the cache is stale. The breakdown replays the eligibility filter and `recompute.applyCapsDetailed` over the whole ledger and then keeps this month's share, so a truncated entry is reported for what it actually paid and the four lines never add up to more than the total above them. Also `getProgressionVisibility(userId)`, the `hideProgression` read that the nav, the dashboard widget and the section's own gate all go through (`FR-12-38`), and `isProgressCacheStale`, the six-hour rule the section's `after()` recompute keys off (`FR-12-11`). `getRankCelebrationContent` resolves what the rank-up celebration renders (the point total the ladder alone cannot supply), and `getWelcomeCelebrationContent` derives the migrated history's one aggregated welcome from two facts that already exist, a `BACKFILL` entry plus a celebration watermark still at zero, rather than from a column of its own (`FR-12-43`), and `getProgressionShellState` answers the hide flag and the pending welcome together out of the single `progression_settings` row the authenticated shell would otherwise read twice on every navigation. Every function takes the session `userId` and nothing else (`BR-12-02`). |
| `src/lib/data/progression/progressionMutations.ts`        | Write side of the points domain. `awardPoints` / `awardPointsBatch` are the only way a ledger row is ever created, idempotent through the `(userId, ruleKey, entityId)` unique key (the batch form resolves the duplicate with `ON CONFLICT DO NOTHING`, so a re-credit cannot abort the host transaction). `voidUserProgressionPoints` is the administrative reversal that keeps the rows and stops counting them. `claimRankCelebration(userId, rankIndex)` is the once-per-rank gate: one `updateMany` on `lastCelebratedRankIndex { lt }` both tests and advances the watermark, so a later recompute that re-derives the same rank claims nothing (`FR-12-19`). `setProgressionVisibility` writes the hide switch (`FR-12-38`); `purgeProgressionLedger` is the collector's own permanent erase of ledger, unlocks, cache and watermark (`FR-12-46`, `BR-12-11`). |
| `src/lib/data/progression/progressionBackfill.ts`         | The one-off credit of the migrated Notion history, as a data-layer function taking a transaction client so the runner can roll it back for a dry run. Writes one synthetic entry set per MIGRATED order (`order-created`, `order-registered`, `order-first-payment`, plus `delivery-received` per delivered delivery) rather than reconstructing two payment events the import fused into one (`FR-12-42`), credits the rest of the history with the normal rules and the real historical `occurredOn`, marks every entry `source = BACKFILL` (`BR-12-12`), recomputes, and pre-marks only the unlocks it just created as `seenAt` + `BACKFILL` so the migration does not replay itself as dozens of toasts (`FR-12-43`). Aborts before any write on `BACKFILL_SOURCE_INCOMPLETE`; re-running is a no-op inherited from the ledger's unique key. Never touches `progression_settings`, which is what leaves the aggregated welcome derivable. Driven by `scripts/backfill-collector-progression.ts` (`--dry-run` by default, `--apply` to commit). |
| `src/lib/imageIntake/imageIntakeMarker.ts`                | The `[image-intake:<digest>]` marker image intake writes into an order's note, and `hasImageIntakeMarker` to recognise one. Shared by the intake save action that writes it and the medal evaluator that reads it at the moment it was written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/pwa/registerServiceWorker.ts`                    | Registers `public/sw.js` from the authenticated app shell, idempotently, failing silently when unsupported. Also kicks off `sweepExpiredShareStash` (unawaited) on every app start, so a share stash abandoned before the intake screen ever mounted is reclaimed from Cache Storage on the next app open regardless of which screen the user lands on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/lib/pwa/shareStash.ts`                               | Client half of the "Share to Panda" hand-off (order image intake, FRD-11): constants and helpers for the Cache Storage bucket the service worker (`public/sw.js`) writes a shared file into before redirecting to the intake screen, which reads and clears it (`readAndClearShareStash`), sweeps an abandoned one (`sweepExpiredShareStash`), and refuses a stash older than the current session (identity cannot be attributed across a sign-in detour). Every constant is mirrored as a literal in `public/sw.js`, a plain-JS static asset that cannot import this module; `shareStash.test.ts` fails if the two drift. `public/sw.js` also runs its own defensive sweep of the same TTL on `activate`, since that is the only lifecycle point guaranteed to run without depending on either app document ever loading.                                                                                                                                                                                       |
| `src/lib/seo.ts`                                          | SEO metadata and canonical URL helpers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/lib/data/stores/storeMatchingQueries.ts`             | Store matching for the image-intake review screen (FRD-11 WO-04): `findStoreMatchesForIntake` promotes the `resolveStore` pattern from `chat-load.ts` into a shared data-layer helper, resolving to `certain` / `ambiguous` / `unknown` from a phone-number and normalized-name signal (`normalizeStoreName`, reused from `src/lib/store/duplicateMatch.ts`). Pairs with `storeMatchingMutations.ts` below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/data/stores/storeMatchingMutations.ts`           | Store writes for the image-intake store step (FRD-11 WO-04): inline creation on an `unknown` match (via the existing `createStore` mutation, so the result is indistinguishable from a manually created store) and remembering a confirmed or corrected phone match on an `ambiguous` one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/store/duplicateMatch.ts`                         | Store name normalization and similarity scoring for duplicate-matching (used by store creation flow). See [Store duplicate detection](store-duplicate-detection.md) for the full blur and submit flow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/store/logo.ts`                                   | Store-logo validation, crop-area parsing, output sizing, and WebP processing helpers shared by the create and edit flows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/lib/store/logoShared.ts`                             | Client-safe store-logo constants (max source size in bytes and MB for UI), schemas, and crop types shared across forms and server actions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/store/logoStorage.ts`                            | S3-compatible object storage helper for uploading and deleting processed store logos.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/lib/store/slug.ts`                                   | Store slug generation: slugify name + 6-char shortId for stable, collision-resistant slugs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/strings/foldSearchText.ts`                       | Case- and diacritic-insensitive fold for search inputs (e.g. autocomplete substring and Enter resolution).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/styles.ts`                                       | Shared class merging (`cn`), gradient bundles (`TINTED_SURFACE_GRADIENT_STOPS`, `TINTED_SURFACE_GRADIENT_TOP_WASH`), and collector shell surfaces (`COLLECTOR_PRIMARY_SECTION_CLASSNAME`, `COLLECTOR_MUTED_INSET_CLASSNAME`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `src/lib/user/avatarProcessing.ts`                        | Server-only avatar file validation, crop-area clamping, and WebP processing using sharp (10 MB source limit, 512px output).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/user/avatarShared.ts`                            | Client-safe avatar constants (accepted MIME types, source size limits, output size and content type), shared `AvatarCropArea` type, MIME guard `isAcceptedAvatarMimeType`, and `normalizeProfileImageUrl` (empty string treated like no image) for settings UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/user/avatarStorage.ts`                           | S3-compatible object storage helper for uploading and deleting user avatar assets under the `user-images/{userId}.webp` stable key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/user-settings/budgetAmount.ts`                   | The single conversion boundary between the whole-currency-unit budget input and the minor-unit `User.budgetAmount` (`toBudgetInputValue`, `parseBudgetInputValue`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/user-settings/budgetCalendar.ts`                 | Budget reset calendar-day resolution (last day of month, clamping).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/user-settings/budgetConstants.ts`                | Budget numeric limits in minor units (`MIN_/MAX_BUDGET_AMOUNT_MINOR`, `BUDGET_MINOR_UNITS_PER_MAJOR`) and reset-day bounds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/lib/user-settings/displayNameRules.ts`               | Display name validation: trim, max-50-char length, reserved-name, PandaTrack brand, and blocked-token protections (token-level, not substring).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/user-settings/collectorPreferencesValidation.ts` | Zod parsing for optional collector preference patches (country, currency, budget, timezone, product type keys).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/lib/user-settings/usernameConstants.ts`              | Shared username numeric limits for validation and generation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `src/lib/user-settings/usernameGeneration.ts`             | Signup username generation with collision retries against the canonical lowercase `username` field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/lib/user-settings/usernameRules.ts`                  | Username format, reserved names, blocked segment rules, and canonical lowercase normalization.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## Related query modules

These live under `src/lib/data/user-settings/` but pair directly with `src/lib/user-settings` contracts:

| File                                                  | Purpose                                                                                                                                        |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/data/user-settings/userSettingsQueries.ts`   | Loads collector preference snapshots and the settings/app-shell identity read models.                                                          |
| `src/lib/data/user-settings/userSettingsMutations.ts` | Applies validated preference patches (scalar `User` fields plus `user_preferred_product_type` links) and the base-currency change transaction. |

## Test fixtures

| File                             | Purpose                                                                          |
| -------------------------------- | -------------------------------------------------------------------------------- |
| `src/test/createTestUserData.ts` | Builds `User` create payloads with valid unique usernames for integration tests. |

## Maintenance rule

Update this file whenever a file in `src/lib/` is added, removed, renamed, or its responsibility changes.
