# Component Map

This file is the catalog of PandaTrack's reusable interface components: what exists, when to use which, where the canonical code lives, and which tokens and decisions govern each. It is the map an agent or contributor consults **before building any UI**, so that new screens reuse the existing system instead of reinventing it.

## How this catalog enforces the visual line

The product's visual consistency is guaranteed by three layers that reinforce each other — none is optional:

1. **This catalog** — the authoritative list of canonical components and their source paths. Reuse before you create.
2. **`docs/design/PLAYBOOK.md`** — the operational workflow, anti-patterns, and self-audit checklist for building UI in this repo.
3. **The `.agents/rules/*.mdc` rules** (several `alwaysApply: true`) — `design-system-playbook.mdc`, `modal-canonical-pattern.mdc`, `ui-libs-policy.mdc`, `optimistic-client-updates.mdc`, `theme-light-dark.mdc`. These are mandatory constraints, not guidance.

**The built component is the source of truth for its own behavior.** For a component that already exists in `src/`, read its code + JSDoc + types for the exact API. This catalog tells you _which_ component to use and _why_; the code tells you _how_. When a component needs a capability it doesn't have, **extend the canonical component in place — never fork it and never roll a parallel one.**

## Reuse rule (mandatory)

Before creating any component:

1. Search this catalog and `src/components/core/` + `src/components/modules/` for an existing one.
2. If one exists, use it. If it's close but missing something, extend it in place.
3. Only if nothing fits, create a new one — in the smallest valid scope (route-level `_components/` first, then `src/components/`), following the existing patterns, theme-safe, with both light and dark covered.
4. Adding a new UI primitive **library** requires an ADR (see [ADR 0010](decisions/0010-ui-primitive-libraries-policy.md)). Hand-roll by default; `vaul` is the only approved primitive lib (used transitively by `<Modal>`'s sheet).

Companion docs: foundations & tokens in [visual-foundations.md](visual-foundations.md) and [tokens-css.md](tokens-css.md); composition patterns in [interface-patterns.md](interface-patterns.md); motion in [motion.md](motion.md); empty/loading/error in [states.md](states.md); copy/voice in [ux-copy.md](ux-copy.md).

---

## Canonical patterns to never reinvent

These have one — and only one — canonical implementation. Copying a similar-looking pattern from anywhere else is a bug.

| Pattern                                        | Canonical component                                       | Rule                                                                                                                                                                               |
| ---------------------------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Any modal / dialog / confirm / form-in-overlay | `<Modal>` — `src/components/modules/Modal/Modal.tsx`      | One modal component; adaptive desktop dialog + mobile sheet. Never a `Portal`+`div`+backdrop. See [modal-canonical-pattern.mdc] + [ADR 0008](decisions/0008-modal-enhancement.md). |
| Mobile bottom sheet                            | `<Sheet>` — `src/components/modules/Sheet/`               | Same Semantic Depth language; the only approved consumer of `vaul`.                                                                                                                |
| List/collection filtering                      | `<FilterDrawer>` — `src/components/modules/FilterDrawer/` | Unified filter surface; paired with `<FilterTriggerButton>` + applied filter chips. Not a `<Modal>`.                                                                               |
| Boolean on/off                                 | `<Switch>` — `src/components/core/Switch.tsx`             | Never `<input type="checkbox">` for a boolean setting.                                                                                                                             |
| Native dropdown                                | `<Select>` / `<Combobox>` / `<SearchableSelect>`          | Never a native `<select>` element.                                                                                                                                                 |
| Empty / loading / error                        | `<EmptyState>` / `<Skeleton>` / `<SectionError>`          | See [states.md](states.md). No ad-hoc per-module skeletons.                                                                                                                        |

---

## Tier 1 — Primitives (`src/components/core/`)

Single-purpose building blocks. Theme-safe, token-driven.

### Typography & identity

| Component    | Path                  | When to use                                                                                                                                          |
| ------------ | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Typography` | `core/Typography.tsx` | Body text scale. The body type authority.                                                                                                            |
| `Heading`    | `core/Heading.tsx`    | Heading scale.                                                                                                                                       |
| `MonoCode`   | `core/MonoCode.tsx`   | Monospace code identifiers (`PT-XXXXXX`); renders in `--text-secondary` per [ADR 0007](decisions/0007-text-muted-outdoor-code-mono-reassignment.md). |
| `Logo`       | `core/Logo.tsx`       | The wordmark. The **only** place Zilla Slab appears (see [visual-foundations.md](visual-foundations.md)).                                            |
| `Eyebrow`    | `core/Eyebrow.tsx`    | Section eyebrow label; part of the Chip-Eyebrow + Top-Accent pattern (see [interface-patterns.md](interface-patterns.md)).                           |

### Actions

| Component                    | Path                                          | When to use                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Button`                     | `core/Button/`                                | All buttons. Variant hierarchy primary / accent / ghost / destructive; one primary per area.                                                                                                                                                                                                                                                                                                                                                                |
| `IconButton`                 | `core/IconButton.tsx`                         | Icon-only action; requires an accessible label. Its `secondary` / `outline` skin is the same pair of tokens as `Button`'s `secondary` — `--surface-elevated` fill, `--border-strong` edge — because both are controls and controls fill elevated ([visual-foundations.md](visual-foundations.md), "Choosing between `--surface` and `--surface-elevated`"). Owns its tap target via `before:[inset:-6px]`, so a caller must not pin its box below `size-8`. |
| `AnchorLink` / `BackNavLink` | `core/AnchorLink.tsx`, `core/BackNavLink.tsx` | In-text link; canonical back/up navigation.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `CodeCopyButton`             | `core/CodeCopyButton.tsx`                     | Copy-to-clipboard for an identifier.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ViewTransitionLink`         | `core/ViewTransitionLink.tsx`                 | List→detail navigation with View Transitions (see [motion.md](motion.md)).                                                                                                                                                                                                                                                                                                                                                                                  |
| `ExpandAllToggle`            | `core/ExpandAllToggle.tsx`                    | Single expand-all / collapse-all toggle for a list of expandable rows; label shows the next action, `aria-pressed` carries the honest tri-state (all / none / mixed).                                                                                                                                                                                                                                                                                       |

### Form controls

| Component                                                | Path                                                                                    | When to use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Input` / `Textarea` / `PasswordInput` / `SearchInput`   | `core/Input.tsx`, `core/Textarea.tsx`, `core/PasswordInput.tsx`, `core/SearchInput.tsx` | Text entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `MoneyAmountInput`                                       | `core/MoneyAmountInput.tsx`                                                             | ONE money amount inside a dense LIST of them (the store payment sheet's allocation rows, the order detail's payment breakdown rows): 44px on touch, 40px from `md`, right-aligned tabular numerals. Replicates `Input`'s visual and a11y contract literally at a density dozens of rows can carry. **Deliberately not a `size` prop on `Input`**, and the argument survived its promotion out of `modules/StorePaymentSheet/`: `InputHTMLAttributes` already defines a native numeric `size`, so the union collapses (TS2322) and fixing it means `Omit`-ing a native attribute from a primitive the whole app consumes, and the height and padding live on an intermediate `<div>` no `className` reaches. Its locked state is `readOnly`, never `disabled`, because a row can lock under a live caret and a field that turns `disabled` there drops focus onto `<body>`, from where the next `Tab` walks out of the modal; `disabled` is offered separately for the one case it fits, a whole surface submitting. For a single prominent amount field, use `Input`. |
| `Select` / `Combobox` / `SearchableSelect`               | `core/Select.tsx`, `core/Combobox.tsx`, `core/SearchableSelect.tsx`                     | Single choice; combobox for type-ahead. Never native `<select>`. On a listing screen, wrap `options` in a single-entry `SelectGroup[]` (`options={[{ heading, options }]}`) so the listbox opens on a heading naming the choice ("Sort by", "Group by"); see [interface-patterns.md](interface-patterns.md#3-buttons-and-interactive-controls). **Trailing cluster: one touch target.** The clear is `size-11` below `md` and `md:size-[18px]` from `md` up; the chevron is decoration (the trigger opens the list) and steps aside below `md` while a clear is on screen. Do not re-add a second control there, and do not spell the compact end as padding — both break the geometry documented in [interface-patterns.md](interface-patterns.md#12-responsive-rules). `Select`'s `onClear` is unsupported at `size="sm"` (a 32px trigger cannot hold a 44px box). `Combobox`'s selected-value chips follow the removable-chip contract below.                                                                                                                      |
| `MultiTagAutocomplete`                                   | `core/MultiTagAutocomplete.tsx`                                                         | Multi-value tag entry. Its in-field chips follow the removable-chip contract (see `Chip`): the remove is a 44×44 box below `md`, with negative margins matching `Chip size="sm"`'s padding.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `Checkbox` / `Radio` / `Switch`                          | `core/Checkbox.tsx`, `core/Radio.tsx`, `core/Switch.tsx`                                | Multi-select / single-select / boolean. Checkbox check uses a hand-rolled 1.5px stroke (see [visual-foundations.md](visual-foundations.md)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ToggleChoiceGroup`                                      | `core/ToggleChoiceGroup.tsx`                                                            | Segmented single choice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `DateInput` / `DatePickerInput` / `DateRangePickerInput` | `core/DateInput.tsx`, `core/DatePickerInput.tsx`, `core/DateRangePickerInput.tsx`       | Date / date-range entry. `DateInput` and `DateRangePickerInput` follow the one-touch-target trailing cluster ([interface-patterns.md](interface-patterns.md#12-responsive-rules)): the clear is a 44×44 box below `md` whose negative margins cancel the trigger's own `px-4 py-3`, so it reaches the field's edges without making the field taller; the calendar glyph steps aside below `md` while a value is set. `DateRangePickerInput`'s clear is invisible to `tap-target-guard` if its icon ever stops being a two-literal ternary — keep it to this rule by hand.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `StarRating` / `RatingStars`                             | `core/StarRating.tsx`, `core/RatingStars.tsx`                                           | Rating display / input. `StarRating` is the canonical, server-safe star renderer (single source of the star visual); `RatingStars` is the client interactive input and **composes** `StarRating` — never redraw stars elsewhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `Label` / `FieldErrorMsg`                                | `core/Label.tsx`, `core/FieldErrorMsg.tsx`                                              | Field label; inline field error.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `Stepper`                                                | `core/Stepper.tsx`                                                                      | Numeric stepper; wizard step indicator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

### Surfaces, status & feedback

| Component                             | Path                                                           | When to use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Card` / `SectionCard`                | `core/Card.tsx`, `core/SectionCard.tsx`                        | Elevated panel; titled section card.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `Chip` / `Pill` / `AppliedFilterChip` | `core/Chip.tsx`, `core/Pill.tsx`, `core/AppliedFilterChip.tsx` | Generic chip; pill; applied-filter chip. **Removable-chip contract:** a chip carrying a remove `<button>` gives it `size-11` below `md` (compact glyph box from `md`) plus negative margins equal to that chip's own padding — `-my-[3px] -mr-[9px]` at `size="md"`, `-my-[var(--space-0_5)] -mr-[var(--space-2)]` at `size="sm"` — so the target reaches the chip's edges and the chip is exactly 44px tall on touch. Never a `::before`: wrapped chip rows sit 4-6px apart, and an expansion there removes a DIFFERENT chip. Changing `Chip`'s padding means changing those margins in `Combobox`, `MultiTagAutocomplete` and `FilterDrawer`. |
| `StatusChip`                          | `core/StatusChip.tsx`                                          | Domain status (order/delivery/item) — enums + neutral + derived per [ADR 0002](decisions/0002-status-chip-mapping.md). Icon+label, never color alone ([ADR 0006](decisions/0006-color-blindness-icon-label-contract.md)).                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ProvenanceValue`                     | `core/ProvenanceValue.tsx`                                     | Attribute whose rendering depends on where its value came from: plain non-focusable text when read, a marked focusable control when assumed or missing. The marker word sits inside the `<label>`, so colour is never the only signal ([ADR 0006](decisions/0006-color-blindness-icon-label-contract.md)).                                                                                                                                                                                                                                                                                                                                      |
| `Avatar` / `StoreAvatar`              | `core/Avatar.tsx`, `core/StoreAvatar.tsx`                      | User avatar; store avatar (neutral mixes use `oklab`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ProgressBar`                         | `core/ProgressBar.tsx`                                         | The only track + fill progress meter in the app (order hero, delivery hero, store-view product rows, store detail payment progress). Gradient fill, `transform: scaleX`, required `valueText` — see the "Progress meters" section of [interface-patterns.md](interface-patterns.md).                                                                                                                                                                                                                                                                                                                                                            |
| `Skeleton`                            | `core/Skeleton.tsx`                                            | Loading placeholder — see [states.md](states.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Toast`                               | `core/Toast/`                                                  | Transient feedback + neutral-undo — see [interface-patterns.md](interface-patterns.md). The dismiss is a 44×44 box with `-my-3 -mr-3` at every breakpoint: the root is `overflow-hidden` (it clips the countdown bar to the rounded corners), which removes a `::before` from hit-testing entirely, so the target grows INWARD over the toast's own `px-4 py-3` instead. Height and message width are unchanged, so do not "restore density" by shrinking it.                                                                                                                                                                                   |
| `Tooltip`                             | `core/Tooltip.tsx`                                             | Hover/focus hint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Portal`                              | `core/Portal.tsx`                                              | Low-level portal primitive (used by overlays; not for hand-rolling modals).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ThemeToggle` / `LangToggle`          | `core/ThemeToggle.tsx`, `core/LangToggle.tsx`                  | Light/dark toggle; locale toggle. `ThemeToggle` is a two-segment pill on `--surface-elevated` with a `--border` edge, the active segment marked by `aria-pressed` plus a 14% accent tint. Its segments are **44×44 below `md` and 26×26 from `md` up** (icon 16 → 13): a segmented control cannot reach the touch floor through a `::before` expansion without one segment eating the other, so the segment itself is sized for touch and only goes compact where the pointer is precise. The public `.mk-theme` copy in `globals.css` follows the same rule at the same breakpoint.                                                            |

---

## Tier 2 — Composite modules (`src/components/modules/`)

Multi-part organisms and orchestration.

### Shell & page chrome

| Component                                     | Path                                                                 | When to use                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Sidebar`                                     | `modules/Sidebar.tsx`                                                | Collector app PUSH sidebar (collapsed rail expands on hover; burger drawer on mobile) — [ADR 0003](decisions/0003-demo-decisions.md).                                                                                                                                                                                                                          |
| `Header`                                      | `modules/Header.tsx`                                                 | App header: breadcrumbs + lang + theme.                                                                                                                                                                                                                                                                                                                        |
| `SectionTitleWithAccent` / `SectionAccentBar` | `modules/SectionTitleWithAccent.tsx`, `modules/SectionAccentBar.tsx` | In-page section title row (accent bar + title).                                                                                                                                                                                                                                                                                                                |
| `Tabs`                                        | `modules/Tabs/`                                                      | Tabbed navigation within a page.                                                                                                                                                                                                                                                                                                                               |
| `CreateOrderFab`                              | `modules/CreateOrderFab/`                                            | The single sanctioned floating action button (FRD-11): labelled "Nuevo pedido" pill, route- and breakpoint-gated (Dashboard + Orders list, below `1024px`). Not a general-purpose FAB primitive — see [interface-patterns.md](interface-patterns.md) "No generic floating action buttons".                                                                     |
| `OrderCreateMethodSelector`                   | `modules/OrderCreateMethodSelector/`                                 | The single "Nuevo pedido" entry point (FRD-11): two cards (from image / manual). `inline` presentation for the orders empty state; `overlay` presentation (wraps the canonical `Modal`) for every other trigger (FAB, dashboard header, store detail, orders toolbar). Every "create order" CTA must open this selector, never link straight to `/orders/new`. |

### Overlays

| Component           | Path                            | When to use                                                                                                                                                                                                             |
| ------------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Modal`             | `modules/Modal/Modal.tsx`       | **The** canonical modal (Semantic Depth, adaptive). Internals: `ModalDialog` (desktop), `ModalSheet` (mobile), `ModalContent` (shared). [ADR 0008](decisions/0008-modal-enhancement.md).                                |
| `Sheet`             | `modules/Sheet/`                | Mobile bottom sheet (same language; only approved `vaul` consumer).                                                                                                                                                     |
| `FilterDrawer`      | `modules/FilterDrawer/`         | List filtering surface. Its selected-value chips follow the removable-chip contract (see `Chip`).                                                                                                                       |
| `MobilePicker`      | `modules/MobilePicker/`         | Mobile-native-style picker.                                                                                                                                                                                             |
| `StoreRemovalModal` | `modules/StoreRemovalModal.tsx` | Store-removal confirmation (`alertdialog`) with a reason radiogroup + optional note. Shared by the store-detail moderation panel and the admin moderation console. Presentational: parent owns Optimistic Confirmation. |

### Content & detail

| Component                                   | Path                                                               | When to use                                                                                                                                                                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `AsideSummary` / `DetailSidebar`            | `modules/AsideSummary/`, `modules/DetailSidebar.tsx`               | Detail-screen sticky rail (Resumen / Acciones / Nota privada).                                                                                                                                                                                         |
| `CollapsibleSection` / `CollapsibleSubcard` | `modules/CollapsibleSection.tsx`, `modules/CollapsibleSubcard.tsx` | Collapsible region; collapsible subcard (state-aware defaults per [ADR 0011](decisions/0011-mobile-detail-secondary-actions.md)).                                                                                                                      |
| `SummaryStatRow`                            | `modules/SummaryStatRow.tsx`                                       | Dashboard micro-stat row ([ADR 0005](decisions/0005-dashboard-microstat-icon-tile.md)).                                                                                                                                                                |
| `PrivateNoteCard`                           | `modules/PrivateNoteCard.tsx`                                      | Private note block on detail screens.                                                                                                                                                                                                                  |
| `ChannelRow`                                | `modules/ChannelRow.tsx`                                           | Store contact/channel row.                                                                                                                                                                                                                             |
| `ListPagination`                            | `modules/ListPagination.tsx`                                       | List pagination (mobile "load more"; desktop summary + page-size select + numbered nav).                                                                                                                                                               |
| `PerPageSelect`                             | `modules/PerPageSelect.tsx`                                        | Desktop page-size control paired with `ListPagination`'s numbered nav.                                                                                                                                                                                 |
| `StoreCombobox`                             | `modules/StoreCombobox/`                                           | Store selection combobox. Below `md` it is a trigger + `MobilePicker`, so the desktop combobox's touch band is 768-1023px: its clear / list-toggle (two arms of one ternary, never siblings) are `size-11` there and go compact at **`lg`**, not `md`. |

### Forms

| Component             | Path                             | When to use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `WizardAccordion`     | `modules/WizardAccordion/`       | Multi-step form (3+ steps); step CTAs never disabled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `FilterTriggerButton` | `core/FilterTriggerButton/`      | Opens the `FilterDrawer`; shows active-filter count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `FaqAccordion`        | `modules/FaqAccordion/`          | FAQ / disclosure list (landing).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ImageCropper`        | `modules/ImageCropper/`          | Avatar / image crop.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ReportReasonPicker`  | `modules/ReportReasonPicker.tsx` | Vertical single-select reason radiogroup (icon + label, ≥44px targets). Shared by the store report and store removal flows and the admin console.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `QuickArrival`        | `modules/QuickArrival/`          | "Ya me llegó": one-step capture of an already-received `entrega` (`FR-08-36`). `QuickArrivalModal` wraps `<Modal>`; the sibling `useQuickArrival` hook is the coordinator every launcher shares (order detail actions cards, dashboard arrival rows). Add a launcher, never a second modal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `StorePaymentSheet`   | `modules/StorePaymentSheet/`     | Records a payment to a store, with an optional declaration of what it covers (§ store-level payments). Wraps `<Modal size="lg">` as **two panels in one modal**: `StorePaymentPanel` (amount, date, note, summary row) and `StorePaymentAllocationPanel` (a flat list of payable lines, one `StorePaymentAllocationRow` each, plus a "Resto del pedido" row per order whose products cannot absorb its balance). Its amount fields are the shared `MoneyAmountInput` (`core/MoneyAmountInput.tsx`), promoted out of this folder in 2026-08 once the order detail's breakdown panel became its second consumer; the reason it is not a `size` prop on `Input` moved with it and is stated there. The sibling `useStorePaymentSheetOrders` hook is the shared fetch-on-open coordinator for both launchers (orders "por tienda" view, store detail page) — each still owns its own submit handler and optimistic patch. Add a launcher, never a second sheet. |

### States, feedback & marketing

| Component         | Path                          | When to use                                                                                                              |
| ----------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `EmptyState`      | `modules/EmptyState.tsx`      | Empty / no-results (and error bases) — [states.md](states.md), [ADR 0013](decisions/0013-cross-cutting-state-system.md). |
| `SectionError`    | `modules/SectionError.tsx`    | In-page section failure with retry — [states.md](states.md).                                                             |
| `AlertBanner`     | `modules/AlertBanner.tsx`     | Inline info / warning / error banner.                                                                                    |
| `OgImageTemplate` | `modules/OgImageTemplate.tsx` | OG image template (see AGENTS.md §OG).                                                                                   |
| `auth/`           | `modules/auth/`               | Auth-flow building blocks.                                                                                               |

---

## Notable route-scoped composites

These live under a route's own `_components/` (per `project-structure.mdc`), not in `src/components/core|modules`, so the inventory guard does not track them — they are page-specific enough that promoting them would be premature. Listed here anyway because they are non-obvious composites future orders/store work should reuse before re-inventing.

| Component                    | Path                                                      | When to use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OrderListGroupBy`           | `orders/_components/OrderListGroupBy.tsx`                 | The Orders list "Por pedido / Por tienda" switch, shown as the ACTIVE VALUE rather than a segmented pair (see [interface-patterns.md](interface-patterns.md#3-buttons-and-interactive-controls) — a low-frequency binary choice reads as a value, not as two competing chips): `variant="select"` reuses the shared `Select` in controlled/grouped mode (desktop toolbar, text only, no icon, no tooltip), `variant="compact"` is a pill trigger + `MobilePicker` sheet (mobile sticky row, same shape as `OrderCurrencyField`/`ItemTypePicker`'s mobile variant). Writes the choice to `?view=` and a cookie so it survives a fresh load.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `StoreGroupedView`           | `orders/_components/StoreGroupedView.tsx`                 | The Orders "Por tienda" list body: one collapsible card per store (local expand state, not `useListExpansion`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `StoreGroupHeader`           | `orders/_components/StoreGroupHeader.tsx`                 | A store group's collapsible header: identity, open-orders/pending-products summary, per-currency debt, "Registrar pago" (disabled pending Fase 4) and "Ver tienda".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `StorePendingProductRow`     | `orders/_components/StorePendingProductRow.tsx`           | Desktop grid row for one pending product inside a store group (Producto / Precio / Estado / Pagado). The third column is headed **"Estado"** and holds the `OrderItemStateChip`, not the arrival: the header was renamed from "Llegada" on 2026-08-13 because "llegada" is now the collector's own act (`glossary.md`), and the arrival lives on line 2 as text (`ArrivalMeta`, below).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `StorePendingProductCard`    | `orders/_components/StorePendingProductCard.tsx`          | Mobile two-line counterpart of `StorePendingProductRow`. Passes `labelDisplay="exceptional"` to `OrderItemStateChip`, the only caller that does: below `md` the default "Pendiente en tienda" row keeps the control and drops the words, and a deviating state keeps its full pill (see [interface-patterns.md §8](interface-patterns.md#8-status-badges-and-chips)).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ArrivalMeta`                | `orders/_components/share/ArrivalMeta.tsx`                | The "Por tienda" row's arrival line, shared by `StorePendingProductRow` and `StorePendingProductCard` so the two cannot drift ([ADR 0030](decisions/0030-arrival-window-shown-at-declared-granularity.md) §8). **One slot of text in every state, one typeface, one size; only the colour changes** — `Llega {ventana}` / `Ya llegó a la tienda` / `Ya está en camino` / `Sin fecha estimada` in `--text-muted`, and `Atrasado {n} días` in **`--warning-chip-text`** (never the raw `--warning`, which is 2.46:1 on `--surface` in light; `design-token-guard` holds this). A late row prints the delay **instead of** the estimate, not beside it, and a row whose arrival has already been OBSERVED prints the event instead of the estimate for the same reason ([ADR 0030](decisions/0030-arrival-window-shown-at-declared-granularity.md) §3.1, 2026-08-17): a line that states a prediction and never names what answered it makes the reader join it with a chip three grid tracks away, which is how the collector came to read a correct row as a defect. It takes the optimistic `deliveryState` as a prop separate from `product`, so the caller must choose which state it is describing rather than inherit the server's — handing the server prop down leaves a delay counter running over a product already on the shelf. Lives in `share/` rather than `src/components/` because it knows this list's own `orderListing.storeView.arrival` namespace.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OrderPaymentBreakdownPanel` | `orders/_components/share/OrderPaymentBreakdownPanel.tsx` | The folded "Desglosar entre productos" block, shared by the order detail's inline payment form ([ADR 0028](decisions/0028-order-scoped-payment-breakdown.md)) and by every payment row of the image-intake review screen ([ADR 0029](decisions/0029-declaring-allocations-against-an-order-that-does-not-exist-yet.md)). Says how much of ONE payment names each product, never what a product still owes. **The layout rule to preserve:** the budget is named ONCE above the list and no row prints a payment-derived figure, so the amounts in the list PARTITION the payment instead of replicating it (six unpriced rows would each advertise the whole payment). One layout for both breakpoints, because the desktop aside is ~300px and the mobile sheet ~327px. It is CONTROLLED: the caller owns `BreakdownPanelState` and reads the same `deriveBreakdown` the panel renders from, so the CTA can never disagree with the foot it sits under. **Two props exist because there are two consumers:** `instanceId` namespaces every DOM id it and its rows mint (`"order-payment"` on the detail, `"intake-payment-<row>"` on the review screen, where several panels are mounted at once and module-constant ids collided across all of them), and `percentBasis` picks which sentence the budget line uses ("% del pedido" vs "% de lo que falta") without entering any formula. The pie **"Marcar todos"** rides on the mode row, or on the `equalOnlyReason` line when the mode chips are not rendered: it ticks every eligible product and lets the split fill the amounts in, pinning nothing. Its opposite already exists in the foot ("Limpiar"). |
| `OrderPaymentBreakdownRow`   | `orders/_components/share/OrderPaymentBreakdownRow.tsx`   | One product inside that panel: checkbox, name, its own STATIC facts (price, what is already declared on it, or "Sin precio"), a `Máx.` fill control carrying the live amount in its accessible name only, and a `MoneyAmountInput`. A product whose price is already covered gets a "Saldado" chip and NO controls at all. Its three id helpers take `(instanceId, itemId)`, because the item key on the review screen is the product's POSITION and is therefore identical on every payment row. Not shared with `StorePaymentAllocationRow`: three structural differences (no per-row figure, a checkbox, no order reference) for two consumers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## Per-component deep specs

Detailed per-component specs (anatomy, all variants, edge cases, full state recipes) were authored during the redesign subproject (historical context only). They are not a source of truth: **the code is authoritative**, and the durable system contract is this catalog plus the topic docs in `docs/design/`.

## Rules & anti-patterns

- **Reuse before create.** A new component that duplicates an existing one is a defect.
- **Never fork a canonical component.** Extend it in place (`<Modal>`, `<Sheet>`, `<FilterDrawer>`, `<Button>`, …).
- **No native `<select>`**, no `Portal`+`div` ad-hoc modals, no `<input type="checkbox">` for booleans.
- **No new UI primitive library** without an ADR ([ADR 0010](decisions/0010-ui-primitive-libraries-policy.md)).
- **Theme-safe always.** Both light and dark, semantic tokens only — no `text-white` / hardcoded hex.
- **Status by icon + label**, never color alone ([ADR 0006](decisions/0006-color-blindness-icon-label-contract.md)).
