# Glossary

This document is the canonical glossary of product terminology used across PandaTrack. Both languages are canonical: every team-facing surface must use these exact terms.

If a term you need is not listed here, add it before introducing inconsistent wording in code, copy, or documentation.

## How to use this glossary

- **Source of truth.** When the product code, copy, or documentation references one of the concepts below, use the exact term in the column for that locale. Do not introduce synonyms.
- **Applies to.** UX copy (`src/i18n/locales/**`), in-product labels, marketing copy, product docs (`docs/product/**`), GitHub issues, FRDs, work orders, ADRs, and PR descriptions.
- **Locale-paired.** When a string exists in both `es` and `en`, use the matching pair from this table. Do not translate between the two using a thesaurus; use this glossary.
- **Code identifiers.** Code identifiers (variables, types, files, routes) follow the **English** column. Repository code is English-only per `english-code-only.mdc`.
- **Update process.** When introducing a new product concept, add the term here in the same change that introduces it. When a term is renamed, update existing strings, identifiers, and docs in the same change and remove the old term.

## Approved terms

| Concept                                                               | Spanish (es)                      | English (en)                    | Notes                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------- | --------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A purchase placed at a store, with one or more items.                 | **pedido**                        | **order**                       | Never use `orden` (es) or `purchase` (en) in user-facing copy or new code. Existing identifiers using `purchase` are legacy; do not extend.                                                                                                                                                                                                                                                 |
| A shipment of one or more order items reaching the user.              | **entrega**                       | **delivery**                    | Never use `envío` (es) or `shipment` (en). Replaces the previous `shipments` route, copy, and identifiers.                                                                                                                                                                                                                                                                                  |
| A merchant or person where pedidos are placed.                        | **tienda**                        | **store**                       | Use even when the seller is an individual (`PERSON` seller type) or an intermediary (`PROXY`). The "tienda" entity/section is distinct from its `sellerType` classification.                                                                                                                                                                                                                |
| How a store sells (`Store.sellerType`, enum `SellerType`).            | **tipo de vendedor**              | **seller type**                 | One of `RETAILER` / `PERSON` / `PROXY`. Renamed from `storeType` / `StoreType` (`BUSINESS` → `RETAILER`), see ADR 0016. Immutable after creation.                                                                                                                                                                                                                                           |
| A business that sells its own products (`RETAILER`).                  | **comercio**                      | **retailer**                    | The `RETAILER` seller type (was `BUSINESS`). Has a catalog, logo, and public contact info.                                                                                                                                                                                                                                                                                                  |
| An individual reseller (`PERSON`).                                    | **persona**                       | **individual**                  | The `PERSON` seller type. Hides public contact info/logo; may be marked private.                                                                                                                                                                                                                                                                                                            |
| A forwarding/intermediary service that buys on your behalf (`PROXY`). | **proxy**                         | **proxy**                       | The `PROXY` seller type (e.g. ZenMarket). Not a direct seller: no catalog (no product types, stock, or pre-orders), keeps logo/contacts/addresses/reviews, always public, badged "Proxy".                                                                                                                                                                                                   |
| A person store visible only to its creator (`Store.isPrivate`).       | **tienda privada**                | **private store**               | Only valid for the `PERSON` seller type (ADR 0009). Excluded from every surface for everyone except its creator, who does see it in their own listing, marked. Say **privada** for the state; the adjective can stand alone as a card marker ("Privada") and takes the explanation only where there is room for it ("Privada, solo tú la ves" on the detail). Avoid `oculta` and `secreta`. |
| A store that has shut down or stopped operating (`isActive: false`).  | **tienda cerrada**                | **closed store**                | Hidden from the public listing by default (opt-in "show closed" filter) and from the order-creation picker; still reachable by direct URL with a warning. Prefer over `tienda inactiva` in user-facing copy.                                                                                                                                                                                |
| A line item inside a pedido.                                          | **producto**                      | **product**                     | Avoid `artículo` / `item` in user-facing copy when referring to an order line; reserve `item` for generic UI lists.                                                                                                                                                                                                                                                                         |
| A pedido with payments tracked over time before fulfillment.          | **pre-reserva**                   | **pre-order**                   | Hyphenated form in English. Spanish form uses `pre-reserva` (with hyphen) consistently. Not a separate entity: it is a pedido with this characteristic, and has no navigation entry or route of its own; it lives inside the Orders section (`BR-03-02`).                                                                                                                                   |
| A monetary movement against a pedido.                                 | **pago**                          | **payment**                     |                                                                                                                                                                                                                                                                                                                                                                                             |
| The user's currency for total roll-ups across stores.                 | **moneda base**                   | **base currency**               |                                                                                                                                                                                                                                                                                                                                                                                             |
| The currency a pedido is denominated in.                              | **moneda**                        | **currency**                    |                                                                                                                                                                                                                                                                                                                                                                                             |
| Creating a pedido by extracting it from a picture.                    | **crear desde imagen**            | **create from image**           | The canonical name of the feature (`FRD-11`). Use it in documentation, settings, quota copy, and analytics. Never "Capturar de WhatsApp": the sources are chats, store emails, web pages, and paper receipts, not one channel. Code prefix `imageIntake` / `OrderImageIntake`; the suffix `Capture` is forbidden (it already means something else in the analytics vocabulary).             |
| The picture an extraction was produced from.                          | **imagen de origen**              | **source image**                | Processed in memory and discarded; only the extracted JSON is persisted. In user-facing copy the everyday word for what the user attaches is **foto**, not "imagen de origen".                                                                                                                                                                                                              |
| The unit the user attaches and the unit the quota counts.             | **foto**                          | **photo**                       | The only word used in the interface for a consumed unit. Never "extracción", "crédito", or "token".                                                                                                                                                                                                                                                                                         |
| The monthly allowance of fotos for image extraction.                  | **cuota de fotos**                | **photo quota**                 | A single flexible monthly bag (20 fotos), spent freely across uploads, resetting on day 1 of the calendar month. Referred to in copy as "tu bolsa mensual". The manual form is never limited by it.                                                                                                                                                                                         |
| The screen that shows what was extracted before anything is saved.    | **Revisa y confirma**             | **Review and confirm**          | Title case as written, used verbatim as the screen title. It is never skipped, at any confidence level (ADR 0021).                                                                                                                                                                                                                                                                          |
| A value the engine defaulted instead of reading from the image.       | **dato asumido**                  | **assumed value**               | Always marked visibly and always editable. Its opposite is a value that was read, which renders as plain, non-focusable text.                                                                                                                                                                                                                                                               |
| The set of productos produced by one phrase in the source image.      | **grupo**                         | **group**                       | The unit of review, not the row: 2 to 5 productos arrive expanded, 6 or more collapsed with a summary. A doubt on any row forces its grupo open.                                                                                                                                                                                                                                            |
| Turning one producto into several, or several into one.               | **separar** / **unir**            | **split** / **merge**           | Bidirectional and general to any producto, not a pack-specific action. The control says "Separar en productos", never "en tomos". Offered only while reviewing an image-intake draft, before the pedido is saved (**ADR 0023**); once saved, the breakdown is corrected by editing the pedido's rows.                                                                                       |
| Sharing an image into PandaTrack from another app.                    | **Compartir a Panda**             | **Share to Panda**              | The OS-level entry point: an Android PWA share target, and an equivalent Shortcut on iOS.                                                                                                                                                                                                                                                                                                   |
| The selector cards for the two ways to create a pedido.               | **Desde una imagen** / **A mano** | **From an image** / **By hand** | The verb lives in the selector title ("Nuevo pedido"); the cards name only the method. Never "Crear a mano" next to "Crear desde imagen": two rival verbs turn one flow into two.                                                                                                                                                                                                           |

## Domain rules tied to terminology

### Producto / product is an atomic shippable unit

Each `producto` (a line inside a `pedido`) represents **one shippable unit**, even when its `cantidad` (quantity) is greater than `1`. The data model treats `producto` as atomic for fulfillment: every `producto` is either fully included in a given `entrega` or not included at all. Fractional fulfillment of a single `producto` across multiple `entrega`s is **not supported**.

Consequences for product surfaces:

- When a user expects different units of the same SKU to arrive in separate `entrega`s, they must enter each unit as a **separate `producto`** with `cantidad = 1` at the time of `pedido` creation. Quantity greater than `1` should be reserved for units that will arrive together.
- The `entrega` create flow must not expose a quantity selector per `producto`; the checkbox represents the entire `producto`.
- The order form must surface this rule contextually (e.g. an inline tooltip near the products section) when at least one `producto` in the form has `cantidad` greater than `1`, so the user has the chance to split before saving.
- If the product later needs partial fulfillment, that is a schema change (adding `quantity` to `DeliveryOrderItem`) tracked in `frd-05` and `frd-08`, not a UI tweak.

## Anti-patterns to avoid

- `Capturar de WhatsApp`, `captura rápida`, `Nuevo desde captura`: retired names for `crear desde imagen`. The feature accepts any image source, so a WhatsApp-specific name lies about what it takes.
- `extracción`, `crédito`, `token` as the user-facing name of a consumed unit: always use `foto`.
- The `Capture` suffix in code identifiers for image-intake work: use `imageIntake` / `OrderImageIntake`. `Capture` already means something else in the repository's analytics vocabulary and creates silent collisions when searching.
- `Separar en tomos`: always `Separar en productos`. `producto` is the canonical name of a pedido line, and "tomos" would tie the interface to manga when PandaTrack targets general collecting.
- `orden` / `órdenes` (es) — always use `pedido` / `pedidos`.
- `envío` / `envíos` (es) — always use `entrega` / `entregas`.
- `shipment` / `shipments` (en) — always use `delivery` / `deliveries`. Marketing copy that references the broader concept of physical shipping is the only allowed exception, but keep the in-app feature name as `delivery`.
- Mixing `purchase` and `order` in the same surface. New work should standardize on `order`.

### Exception: "envío" / "shipping" as an attribute, not an entity

The ban above applies to `envío` / `shipping` when it names the **feature** (the entity is `entrega` / `delivery`). It does not apply when the word describes an **attribute** of a `delivery`, such as a shipping date or a shipping cost — English commonly qualifies these as "shipping date" / "shipping cost" the same way Spanish uses "fecha de envío" / "costo de envío". Existing attribute-level labels in `deliveries.json` and `orders.json` (for example a shipping-date or shipping-cost field label) are correct as written and do not need to be renamed to "delivery date" / "delivery cost". Only the entity name is reserved for `entrega` / `delivery`; do not introduce a new top-level "envío" / "shipment" feature, route, or model.

## Cross-references

- UX voice and writing rules: `docs/design/ux-copy.md`.
- Repository implementation rules that depend on this glossary: `.agents/rules/role-copywriting-marketing.mdc`, `.agents/rules/english-code-only.mdc`.
- Locale files: `src/i18n/locales/{es,en}/*.json`.
