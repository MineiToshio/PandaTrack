export { default as QuickArrivalModal } from "./QuickArrivalModal";
export type { QuickArrivalItem, QuickArrivalModalProps } from "./QuickArrivalModal";
/** The modal's `onSubmit` payload. Type-only, so the per-order hook stays out of other bundles. */
export type { QuickArrivalSubmitInput } from "./useQuickArrival";
